// Pure helpers — no RN/Expo imports, no business logic.
// `lib/mock/fixtures` is intentionally NOT re-exported from this barrel:
// it is mock seed data for BIZ-230 / sub-issue dev, not production code.
export {};
