// Infrastructure layer — adapters that implement application ports.
// Foundation (BIZ-268) declares the barrel only; concrete adapters
// (`BotApiClient`, `AuthClient`, `SecureTokenStore`, `ChatDatabase`,
// `TraceStreamClient`) land in M1 sub 2 / sub 3 / sub 6.
export {};
