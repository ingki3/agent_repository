// Domain layer (pure TS) — entities / value-objects / rules.
// Foundation (BIZ-268) declares the barrel only; concrete exports land in
// M1 sub 3 (BIZ-272) when `Buddy` / `Message` / `Trace` entities are written.
export {};
